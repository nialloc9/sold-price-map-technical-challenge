{
  "name": "server",
  "version": "1.0.0",
  "main": "handler.js",
  "license": "MIT",
  "scripts": {
    "compile": "rollup -c",
    "upload": "serverless deploy -v",
    "deploy": "yarn compile && yarn upload",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.542.0",
    "line-by-line": "^0.1.6"
  },
  "devDependencies": {
    "rollup": "^1.23.0",
    "serverless": "^1.53.0",
    "jest": "^24.9.0"
  }
}
